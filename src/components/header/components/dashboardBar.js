import React, { memo } from 'react'
import {  
  StyledDashboardBar
} from '../style';

const DashboardBar = () => {
  return (
    <StyledDashboardBar>
      
    </StyledDashboardBar>
  );
}

export default memo(DashboardBar);